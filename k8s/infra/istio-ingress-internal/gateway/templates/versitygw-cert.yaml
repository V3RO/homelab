apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: versitygw-schober-dev-tls
  namespace: {{ .Release.Namespace }}
spec:
  issuerRef:
    name: cloudflare-letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - "*.versitygw.schober.dev"
  secretName: versitygw-schober-dev-tls
