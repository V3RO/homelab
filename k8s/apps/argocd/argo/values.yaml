argo-cd:
  global:
    domain: argo.schober.dev

  configs:
    params:
      server.insecure: true

  server:
    httproute:
      enabled: true
      parentRefs:
        - name: internal-gateway
          namespace: istio-ingress-internal
          sectionName: https