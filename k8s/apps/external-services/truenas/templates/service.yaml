apiVersion: v1
kind: Service
metadata:
  name: truenas
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - name: https
      port: {{ .Values.truenas.port }}
      targetPort: {{ .Values.truenas.port }}
      protocol: TCP
---
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: truenas
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - truenas.{{ .Release.Namespace }}.svc.cluster.local
  location: MESH_EXTERNAL
  ports:
    - name: https
      number: {{ .Values.truenas.port }}
      protocol: TLS
  resolution: STATIC
  endpoints:
    - address: {{ .Values.truenas.ip }}
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: truenas
  namespace: {{ .Release.Namespace }}
spec:
  host: truenas.{{ .Release.Namespace }}.svc.cluster.local
  trafficPolicy:
    tls:
      mode: SIMPLE
      insecureSkipVerify: true
